module test;
reg reset,enable,clk;
wire [3:0]count;

counter abc (.reset(reset), .enable(enable), .clk(clk), .count(count));

always 
#5 clk = ~ clk ;

initial begin
clk =0;
reset = 1;
enable = 0;

#10;
reset = 0;
enable = 1;

#50; 
enable = 0;

#10; 
enable = 1;

#70;
enable = 0;

#10; enable = 1;

#30; reset = 1;

#10; reset = 0 ;
enable = 1;


#200 $finish;
end
endmodule
